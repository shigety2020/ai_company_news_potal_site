# AI社員デイリー — プロダクトバックログ

価値順。完了済みの誌面骨格は除外。ログイン／コメントは後回し。

最終更新: 2026-09-08（Sprint 6 Done）

スクラムイベント記録: [`docs/scrum/`](./docs/scrum/README.md)

## プロダクトビジョン

毎日 X から「AI社員／AIエージェント／デジタル社員」の作り方・ツール・事例を集め、真っ白な編集誌面で届けるポータル。

## 契約（触らない）

`GET /api/daily?date=YYYY-MM-DD` → `{ date, featured, items[] }`

各 item: `{ time, headline, summary, handle, category, url }`  
category: `作り方` / `ツール` / `事例`

## バックログ（価値順）

| # | 項目 | 状態 | Done（受け入れ） |
|---|------|------|------------------|
| 1 | X日次収集を同じJSONのまま誌面に乗せる | **Done**（Sprint 1） | 実Xデータが特集＋目次に出る。契約不変。 |
| 2 | 特集の選び方を実サンプルで確定 | **Done**（Sprint 2） | 「読みやすい作り方」優先。ロック済み。 |
| 3 | 1号あたり特集1＋目次20まで厚くする | **Done**（Sprint 3） | JSONは最大20。画面はカテゴリ各3件まで。 |
| 4 | 日付で号を送る（前日／翌日） | **Done**（Sprint 4） | 日付指定で同じ誌面が開く。無い日は `featured: null`＋空目次。マストヘッドは残し「今日の特集はありません」。契約・レイアウトは触らない。 |
| 5 | 日次収集を日付キーの号に乗せる（手動シード卒業） | **Done**（Sprint 5） | 日付キーの号が日次収集から載る。契約は `GET /api/daily?date=` のまま。無い日は空号。レイアウト／画面3件キャップは触らない。収集前に残高確認。 |
| 6 | 毎朝自動で今日の号を載せる（人手pushなし） | **Done**（Sprint 6） | 毎朝（JST）人手のpushなしで、その日の号が `GET /api/daily?date=今日` に載る。無い日は空号のまま。契約・レイアウト・画面3件キャップは触らない。収集前に残高確認。失敗したら空号を壊さない。 |

## 後回し

- ログイン
- コメント

## シゲッティ向けの見方

このファイルがバックログの正。イベント記録は `docs/scrum/`。チャットと食い違うときはここを更新してから話す。
次の価値項目はレビュー／レトロ後にリクが切る。
