# Sprint 6 レトロ — 2026-09-08

## Keep / Problem / Try（1人1行）

- **ナオ** Keep: `/api/daily` に乗ったら自分から確認して画素へ渡せた。Problem: 確認がプレビュー枝前提。Try: 本番URLでも同じ `?date=` と3件キャップで読む。
- **レン** Keep: 明朝自動で残高→push→特集1＋20を返してから渡せた。Problem: 載せ先が骨格プレビュー枝。Try: 本番でも同じ `/api/daily?date=` に載せる。
- **ユイ** Keep: Ready出たら自分から号あり／空号を通せた。Problem: 画素がプレビューURL前提。Try: 本番でも同じ空号コピーとナビで通す。
- **カナ** Keep: Ready出たら自分から号あり／空号を通せた。Problem: 回帰がプレビューURL前提。Try: 本番でも同じDoneで通す。
- **リク** Keep: Doneを先に切って受け入れた。Problem: 読者向け本番URLがまだ無い。Try: PB#7を本番URLにする。

## ロックした Try

1. 本番URLでも同じ `?date=` と3件キャップで読む（ナオ）
2. 本番でも同じ `/api/daily?date=` に載せる（レン）
3. 本番URLでも同じ空号コピーとナビ／Doneで通す（ユイ・カナ）

## 次

PB#7 Ready。Sprint 7 プランニングへ。
