# Sprint 7 レトロ — 2026-09-08

## Keep / Problem / Try（1人1行）

- **ナオ** Keep: 本番でも同じ `?date=` と3件キャップで通せた。Problem: PR権限でマージ待ち。Try: 翌朝の号確認は本番URL。
- **レン** Keep: 収集先を `main`／本番に切替できた。Problem: PR権限でマージ待ち。Try: 明朝自動は本番で特集1＋20を返してから渡す。
- **ユイ** Keep: 本番で号あり／空号を通せた。Problem: PR権限でマージ待ち。Try: 画素は本番URL。空号画像は明日リファインメント。
- **カナ** Keep: 本番回帰を通せた。Problem: PR権限でマージ待ち。Try: 朝の回帰を本番URLに切替済み。
- **リク** Keep: Doneを先に切って本番まで乗せた。Problem: PR作成が権限で止まった。Try: 明日リファインメントで次の種を切る。

## ロックした Try

1. 翌朝の号確認は本番URL（ナオ）
2. 明朝自動は本番で特集1＋20を返してから渡す（レン）
3. 画素・朝回帰は本番URL（ユイ・カナ）
4. 明日リファインメントで空号画像ほか次の種を切る（リク）

## 次

明日リファインメントから。X MVP一区切り済み。
